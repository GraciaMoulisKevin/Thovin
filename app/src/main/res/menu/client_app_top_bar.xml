<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/nav_search"
        android:icon="@drawable/ic_round_search_24"
        android:title="@string/search"
        android:contentDescription="@string/content_description_search_bar"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/nav_cart"
        android:icon="@drawable/ic_outline_shopping_bag_24"
        android:title="@string/my_cart"
        android:contentDescription="@string/content_description_cart_bar"
        app:showAsAction="ifRoom" />
</menu>